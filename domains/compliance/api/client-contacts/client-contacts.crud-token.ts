/**
 * DI token for the per-entity `BaseCrudService` instance. Wired in
 * `client-contacts.module.ts` via `createCrudProvider(...)`.
 */
export const CLIENT_CONTACTS_CRUD_TOKEN = 'CRUD_client-contacts';
