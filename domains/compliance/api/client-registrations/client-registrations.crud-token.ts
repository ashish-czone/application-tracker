/**
 * DI token for the per-entity `BaseCrudService` instance. Wired in
 * `client-registrations.module.ts` via `createCrudProvider(...)`.
 */
export const CLIENT_REGISTRATIONS_CRUD_TOKEN = 'CRUD_client-registrations';
