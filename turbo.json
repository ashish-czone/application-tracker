{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["APP_DOMAIN"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["APP_DOMAIN"]
    },
    "lint": {},
    "test": {}
  }
}
