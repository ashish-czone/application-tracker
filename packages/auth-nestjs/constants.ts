import type { AuthModuleConfig } from '@packages/auth';

export const IS_PUBLIC_KEY = 'isPublic';
export const AUTH_MODULE_CONFIG = 'AUTH_MODULE_CONFIG';

export const AUTH_CONFIGS_MAP = new Map<string, AuthModuleConfig>();
